[project]
name = "ucb-mids-w205-repo"
version = "0.1.0"
description = "A non-package project for MIDS W205"
authors = [{name = "David Lin"}]
requires-python = "~=3.10.0"  # This means >=3.10.0, <3.11.0
dependencies = [
    "paramiko>=3.5.1",
    "ipykernel>=6.29.5",
    "pandas>=2.3.0",
    "matplotlib>=3.10.3",
]

[tool.uv]
dev-dependencies = []

[tool.hatch.build.targets.wheel]
packages = []